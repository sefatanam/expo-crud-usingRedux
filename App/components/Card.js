import React from "react";
import { View, Text, StyleSheet, Button } from "react-native";
const Card = () => {
  return (
    <View>
      <Text>Card</Text>
      <Button title="Go Back" />
    </View>
  );
};

export default Card;
